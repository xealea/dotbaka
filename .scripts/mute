XF86AudioMute
if ou_mute=$(pamixer -t --get-mute); then
    if [ "$ou_mute" = "true" ]; then
        printf -v msg "%s\n%s" "  Speaker" "MUTED"
    else
        printf -v msg "%s\n%s" "  Speaker" "UNMUTED"
    fi
    dunstify "$msg" -t 1000 -r 1
fi
